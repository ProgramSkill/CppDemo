# Pandas

Pandas数据处理模块，涵盖DataFrame、数据清洗和分析。

## 📚 内容概览

| 主题 | 描述 | 难度 |
|------|------|------|
| DataFrame | 创建、读取、写入 | ⭐ |
| 数据选择 | loc, iloc, 布尔索引 | ⭐⭐ |
| 数据清洗 | 缺失值、重复值处理 | ⭐⭐ |
| 数据聚合 | groupby, pivot | ⭐⭐⭐ |

## 🎯 学习目标

完成本模块后，你将能够：
- 创建和操作DataFrame
- 进行数据选择和过滤
- 处理缺失数据
- 进行数据聚合和分析

## 💡 核心示例

```python
import pandas as pd

# 创建DataFrame
df = pd.DataFrame({
    'Name': ['Alice', 'Bob', 'Charlie'],
    'Age': [25, 30, 35],
    'Salary': [50000, 60000, 70000]
})

# 数据操作
print(df[df['Age'] > 25])           # 过滤
print(df.groupby('Age').mean())     # 聚合
```

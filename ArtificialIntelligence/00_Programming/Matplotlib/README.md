# Matplotlib

Matplotlib数据可视化模块，涵盖图表绑定和自定义。

## 📚 内容概览

| 主题 | 描述 | 难度 |
|------|------|------|
| 基础图表 | 折线图、散点图、柱状图 | ⭐ |
| 统计图表 | 直方图、箱线图 | ⭐⭐ |
| 子图布局 | subplot, subplots | ⭐⭐ |
| 高级定制 | 样式、注释、3D图 | ⭐⭐⭐ |

## 🎯 学习目标

完成本模块后，你将能够：
- 创建各种类型的图表
- 自定义图表样式
- 创建多子图布局
- 保存高质量图像

## 💡 核心示例

```python
import matplotlib.pyplot as plt
import numpy as np

x = np.linspace(0, 10, 100)
y = np.sin(x)

plt.figure(figsize=(10, 6))
plt.plot(x, y, label='sin(x)')
plt.xlabel('x')
plt.ylabel('y')
plt.title('Sine Function')
plt.legend()
plt.grid(True)
plt.show()
```
